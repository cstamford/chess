namespace Chess.Representation
{
    /// <summary>
    /// Represents one of two possible teams in a game of chess, 
    /// or optionally an invalid state.
    /// </summary>
    public enum Team
    {
        Black,
        White,
        Invalid
    }
}